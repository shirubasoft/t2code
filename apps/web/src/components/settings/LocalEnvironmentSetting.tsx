import { SettingsRow } from "./settingsLayout";
export function LocalEnvironmentSetting() {
  return (
    <SettingsRow
      title="Local environment"
      description="Agents and projects run on this computer."
    />
  );
}
