{
  "replacements": [
    {
      "path": "apps/desktop/src/app/DesktopObservability.ts",
      "before": "import { PRIMARY_LOCAL_ENVIRONMENT_ID } from \"@t3tools/contracts\";\nimport {\n  makeLocalFileTracer,\n  makeTraceSink,\n  otlpSerializationLayer,\n",
      "after": "import { allowAnalyticsExport } from \"@t3tools/shared/t2Analytics\";\nimport { PRIMARY_LOCAL_ENVIRONMENT_ID } from \"@t3tools/contracts\";\nimport {\n  makeLocalFileTracer,\n  makeTraceSink,\n  otlpSerializationLayer,\n"
    },
    {
      "path": "apps/desktop/src/app/DesktopObservability.ts",
      "before": "        const delegate =\n          endpoints.traces === undefined\n",
      "after": "        const delegate =\n          !allowAnalyticsExport() || endpoints.traces === undefined\n"
    },
    {
      "path": "apps/desktop/src/electron/ElectronUpdater.ts",
      "before": "import * as Layer from \"effect/Layer\";\nimport * as Schema from \"effect/Schema\";\nimport * as Scope from \"effect/Scope\";\n\nimport { autoUpdater } from \"electron-updater\";\n\ntype AutoUpdater = typeof autoUpdater;\n\nexport type ElectronUpdaterFeedUrl = Parameters<AutoUpdater[\"setFeedURL\"]>[0];\n\n",
      "after": "import * as Layer from \"effect/Layer\";\nimport * as Schema from \"effect/Schema\";\nimport * as Scope from \"effect/Scope\";\n\nimport { autoUpdater } from \"electron-updater\";\nimport { disableUpdateTracking } from \"./t2UpdaterPrivacy.ts\";\n\ntype AutoUpdater = typeof autoUpdater;\n\nexport type ElectronUpdaterFeedUrl = Parameters<AutoUpdater[\"setFeedURL\"]>[0];\n\n"
    },
    {
      "path": "apps/desktop/src/electron/ElectronUpdater.ts",
      "before": "    Effect.suspend(() => {\n      autoUpdater.disableDifferentialDownload = value;\n      return Effect.void;\n    }),\n  checkForUpdates: Effect.suspend(() => {\n    const channel = autoUpdater.channel;\n    return Effect.tryPromise({\n      try: () => autoUpdater.checkForUpdates(),\n      catch: (cause) => new ElectronUpdaterCheckForUpdatesError({ channel, cause }),\n    }).pipe(Effect.asVoid);\n",
      "after": "    Effect.suspend(() => {\n      autoUpdater.disableDifferentialDownload = value;\n      return Effect.void;\n    }),\n  checkForUpdates: Effect.suspend(() => {\n    disableUpdateTracking(autoUpdater);\n    const channel = autoUpdater.channel;\n    return Effect.tryPromise({\n      try: () => autoUpdater.checkForUpdates(),\n      catch: (cause) => new ElectronUpdaterCheckForUpdatesError({ channel, cause }),\n    }).pipe(Effect.asVoid);\n"
    },
    {
      "path": "apps/mobile/src/features/cloud/CloudAuthProvider.tsx",
      "before": "  if (!publishableKey || !relayUrl) {\n    return props.children;\n  }\n\n  return (\n    <ClerkProvider publishableKey={publishableKey} tokenCache={tokenCache}>\n      <CloudAuthBridge>{props.children}</CloudAuthBridge>\n    </ClerkProvider>\n  );\n}\n",
      "after": "  if (!publishableKey || !relayUrl) {\n    return props.children;\n  }\n\n  return (\n    <ClerkProvider telemetry={false} publishableKey={publishableKey} tokenCache={tokenCache}>\n      <CloudAuthBridge>{props.children}</CloudAuthBridge>\n    </ClerkProvider>\n  );\n}\n"
    },
    {
      "path": "apps/mobile/src/features/observability/tracing.test.ts",
      "before": "      extra: {},\n    },\n  },\n}));\n\nit.effect(\"exports spans through the scoped mobile OTLP layer\", () => {\n  const fetchFn = vi.fn<typeof fetch>(async () => new Response(null, { status: 202 }));\n  const tracingLayer = makeTracingLayer(\n    {\n      tracesUrl: \"https://api.axiom.test/v1/traces\",\n      tracesDataset: \"mobile-traces\",\n",
      "after": "      extra: {},\n    },\n  },\n}));\n\nit.effect(\"does not export spans through the configured mobile OTLP layer\", () => {\n  const fetchFn = vi.fn<typeof fetch>(async () => new Response(null, { status: 202 }));\n  const tracingLayer = makeTracingLayer(\n    {\n      tracesUrl: \"https://api.axiom.test/v1/traces\",\n      tracesDataset: \"mobile-traces\",\n"
    },
    {
      "path": "apps/mobile/src/features/observability/tracing.test.ts",
      "before": "    expect(fetchFn).not.toHaveBeenCalled();\n  }).pipe(\n    Effect.scoped,\n    Effect.andThen(\n      Effect.sync(() => {\n        expect(fetchFn).toHaveBeenCalledOnce();\n        const [url, init] = fetchFn.mock.calls[0]!;\n        expect(String(url)).toBe(\"https://api.axiom.test/v1/traces\");\n        expect(new Headers(init?.headers).get(\"authorization\")).toBe(\"Bearer public-ingest-token\");\n        expect(new Headers(init?.headers).get(\"x-axiom-dataset\")).toBe(\"mobile-traces\");\n        expect(new TextDecoder().decode(init?.body as Uint8Array)).toContain(\"mobile.test.span\");\n      }),\n    ),\n  );\n});\n\nit.effect(\"does not let OTLP serialization failures alter application effects\", () => {\n  const fetchFn = vi.fn<typeof fetch>(async () => new Response(null, { status: 202 }));\n  const tracingLayer = makeTracingLayer(\n    {\n      tracesUrl: \"https://api.axiom.test/v1/traces\",\n      tracesDataset: \"mobile-traces\",\n",
      "after": "    expect(fetchFn).not.toHaveBeenCalled();\n  }).pipe(\n    Effect.scoped,\n    Effect.andThen(\n      Effect.sync(() => {\n        expect(fetchFn).not.toHaveBeenCalled();\n      }),\n    ),\n  );\n});\n\nit.effect(\"preserves application failures without exporting them\", () => {\n  const fetchFn = vi.fn<typeof fetch>(async () => new Response(null, { status: 202 }));\n  const tracingLayer = makeTracingLayer(\n    {\n      tracesUrl: \"https://api.axiom.test/v1/traces\",\n      tracesDataset: \"mobile-traces\",\n"
    },
    {
      "path": "apps/mobile/src/features/observability/tracing.test.ts",
      "before": "\n  return Layer.build(tracedApplication).pipe(\n    Effect.scoped,\n    Effect.andThen(\n      Effect.sync(() => {\n        expect(fetchFn).toHaveBeenCalledOnce();\n        expect(new TextDecoder().decode(fetchFn.mock.calls[0]?.[1]?.body as Uint8Array)).toContain(\n          \"mobile.test.failed-span\",\n        );\n      }),\n    ),\n  );\n});\n",
      "after": "\n  return Layer.build(tracedApplication).pipe(\n    Effect.scoped,\n    Effect.andThen(\n      Effect.sync(() => {\n        expect(fetchFn).not.toHaveBeenCalled();\n      }),\n    ),\n  );\n});\n"
    },
    {
      "path": "apps/server/src/http.ts",
      "before": "  AuthOrchestrationReadScope,\n  EnvironmentHttpApi,\n} from \"@t3tools/contracts\";\nimport { isDevProxiedPath } from \"@t3tools/shared/devProxy\";\nimport { decodeOtlpTraceRecords } from \"@t3tools/shared/observability\";\nimport * as Data from \"effect/Data\";\nimport * as Effect from \"effect/Effect\";\nimport * as FileSystem from \"effect/FileSystem\";\nimport * as Layer from \"effect/Layer\";\nimport * as Option from \"effect/Option\";\n",
      "after": "  AuthOrchestrationReadScope,\n  EnvironmentHttpApi,\n} from \"@t3tools/contracts\";\nimport { isDevProxiedPath } from \"@t3tools/shared/devProxy\";\nimport { decodeOtlpTraceRecords } from \"@t3tools/shared/observability\";\nimport { allowAnalyticsExport } from \"@t3tools/shared/t2Analytics\";\nimport * as Data from \"effect/Data\";\nimport * as Effect from \"effect/Effect\";\nimport * as FileSystem from \"effect/FileSystem\";\nimport * as Layer from \"effect/Layer\";\nimport * as Option from \"effect/Option\";\n"
    },
    {
      "path": "apps/server/src/http.ts",
      "before": "          bodyJson,\n        }),\n      ),\n    );\n\n    if (otlpTracesUrl === undefined) {\n      return HttpServerResponse.empty({ status: 204 });\n    }\n\n    return yield* httpClient\n      .post(otlpTracesUrl, {\n",
      "after": "          bodyJson,\n        }),\n      ),\n    );\n\n    if (!allowAnalyticsExport() || otlpTracesUrl === undefined) {\n      return HttpServerResponse.empty({ status: 204 });\n    }\n\n    return yield* httpClient\n      .post(otlpTracesUrl, {\n"
    },
    {
      "path": "apps/server/src/observability/Layers/Observability.ts",
      "before": "import { httpHeaderRedactionLayer } from \"@t3tools/shared/httpObservability\";\nimport {\n  makeLocalFileTracer,\n  makeTraceSink,\n  otlpSerializationLayer,\n",
      "after": "import { allowAnalyticsExport } from \"@t3tools/shared/t2Analytics\";\nimport { httpHeaderRedactionLayer } from \"@t3tools/shared/httpObservability\";\nimport {\n  makeLocalFileTracer,\n  makeTraceSink,\n  otlpSerializationLayer,\n"
    },
    {
      "path": "apps/server/src/observability/Layers/Observability.ts",
      "before": "              count: stats.count,\n              durationMs: stats.durationMs,\n            }),\n        });\n        const delegate =\n          config.otlpTracesUrl === undefined\n            ? undefined\n            : yield* OtlpTracer.make({\n                url: config.otlpTracesUrl,\n                exportInterval: `${config.otlpExportIntervalMs} millis`,\n                headers: config.otlpHeaders,\n",
      "after": "              count: stats.count,\n              durationMs: stats.durationMs,\n            }),\n        });\n        const delegate =\n          !allowAnalyticsExport() || config.otlpTracesUrl === undefined\n            ? undefined\n            : yield* OtlpTracer.make({\n                url: config.otlpTracesUrl,\n                exportInterval: `${config.otlpExportIntervalMs} millis`,\n                headers: config.otlpHeaders,\n"
    },
    {
      "path": "apps/server/src/observability/Layers/Observability.ts",
      "before": "        );\n      }),\n    ).pipe(Layer.provide(OtlpExporter.layerFlusher), Layer.provideMerge(serializationLayer));\n\n    const metricsLayer =\n      config.otlpMetricsUrl === undefined\n        ? Layer.empty\n        : OtlpMetrics.layer({\n            url: config.otlpMetricsUrl,\n            exportInterval: `${config.otlpExportIntervalMs} millis`,\n            headers: config.otlpHeaders,\n",
      "after": "        );\n      }),\n    ).pipe(Layer.provide(OtlpExporter.layerFlusher), Layer.provideMerge(serializationLayer));\n\n    const metricsLayer =\n      !allowAnalyticsExport() || config.otlpMetricsUrl === undefined\n        ? Layer.empty\n        : OtlpMetrics.layer({\n            url: config.otlpMetricsUrl,\n            exportInterval: `${config.otlpExportIntervalMs} millis`,\n            headers: config.otlpHeaders,\n"
    },
    {
      "path": "apps/server/src/server.test.ts",
      "before": "        assert.equal(response.environment.environmentId, testEnvironmentDescriptor.environmentId);\n        assert.equal(response.auth.policy, \"desktop-managed-local\");\n      }).pipe(Effect.provide(NodeHttpServer.layerTest)),\n  );\n\n  it.effect(\"proxies browser OTLP trace exports through the server\", () =>\n    Effect.gen(function* () {\n      const upstreamRequests: Array<{\n        readonly body: string;\n        readonly contentType: string | null;\n      }> = [];\n",
      "after": "        assert.equal(response.environment.environmentId, testEnvironmentDescriptor.environmentId);\n        assert.equal(response.auth.policy, \"desktop-managed-local\");\n      }).pipe(Effect.provide(NodeHttpServer.layerTest)),\n  );\n\n  it.effect(\"keeps browser OTLP traces local with a configured JSON exporter\", () =>\n    Effect.gen(function* () {\n      const upstreamRequests: Array<{\n        readonly body: string;\n        readonly contentType: string | null;\n      }> = [];\n"
    },
    {
      "path": "apps/server/src/server.test.ts",
      "before": "          status: {\n            code: \"STATUS_CODE_OK\",\n          },\n        },\n      ]);\n      assert.deepEqual(upstreamRequests, [\n        {\n          body: jsonRequestBody(payload),\n          contentType: \"application/json\",\n        },\n      ]);\n    }).pipe(Effect.provide(NodeHttpServer.layerTest)),\n  );\n\n  it.effect(\"forwards browser OTLP traces as protobuf when the protocol is http/protobuf\", () =>\n    Effect.gen(function* () {\n      const upstreamRequests: Array<{\n        readonly body: string;\n        readonly contentType: string | null;\n      }> = [];\n",
      "after": "          status: {\n            code: \"STATUS_CODE_OK\",\n          },\n        },\n      ]);\n      assert.deepEqual(upstreamRequests, []);\n    }).pipe(Effect.provide(NodeHttpServer.layerTest)),\n  );\n\n  it.effect(\"keeps browser OTLP traces local with a configured protobuf exporter\", () =>\n    Effect.gen(function* () {\n      const upstreamRequests: Array<{\n        readonly body: string;\n        readonly contentType: string | null;\n      }> = [];\n"
    },
    {
      "path": "apps/server/src/server.test.ts",
      "before": "        // @effect-diagnostics-next-line preferSchemaOverJson:off\n        body: HttpBody.text(JSON.stringify(payload), \"application/json\"),\n      });\n\n      assert.equal(response.status, 204);\n      // The local collector still decodes the browser's JSON before forwarding.\n      assert.equal(localTraceRecords.length, 1);\n      assert.equal(upstreamRequests.length, 1);\n      const forwarded = upstreamRequests[0];\n      assert.notEqual(forwarded, undefined);\n      if (!forwarded) {\n        return;\n      }\n      assert.equal(forwarded.contentType, \"application/x-protobuf\");\n      // Protobuf strings are raw UTF-8, so the span and service names survive\n      // the stub's utf8 decode even though the surrounding bytes don't.\n      assert.notEqual(forwarded.body[0], \"{\");\n      assert.include(forwarded.body, \"client.protobuf.test\");\n      assert.include(forwarded.body, \"t3-web\");\n    }).pipe(Effect.provide(NodeHttpServer.layerTest)),\n  );\n\n  it.effect(\"responds to browser OTLP trace preflight requests with CORS headers\", () =>\n    Effect.gen(function* () {\n",
      "after": "        // @effect-diagnostics-next-line preferSchemaOverJson:off\n        body: HttpBody.text(JSON.stringify(payload), \"application/json\"),\n      });\n\n      assert.equal(response.status, 204);\n      // The local collector still decodes the browser's JSON without forwarding it.\n      assert.equal(localTraceRecords.length, 1);\n      assert.deepEqual(upstreamRequests, []);\n    }).pipe(Effect.provide(NodeHttpServer.layerTest)),\n  );\n\n  it.effect(\"responds to browser OTLP trace preflight requests with CORS headers\", () =>\n    Effect.gen(function* () {\n"
    },
    {
      "path": "apps/server/src/telemetry/AnalyticsService.test.ts",
      "before": "      };\n    }>;\n  } | null;\n}\n\ninterface RecordedBatchBody {\n  readonly batch: ReadonlyArray<{\n    readonly event?: string;\n    readonly properties?: {\n      readonly index?: number;\n      readonly clientType?: string;\n      readonly serverOs?: string;\n      readonly serverArch?: string;\n      readonly serverAppVersion?: string;\n      readonly serverMode?: string;\n      readonly t3CodeVersion?: string;\n    };\n  }>;\n}\n\nit.layer(NodeServices.layer)(\"AnalyticsService test\", (it) => {\n  it.effect(\"flush drains all buffered events across multiple batches\", () =>\n    Effect.gen(function* () {\n      const capturedRequests: Array<RecordedBatchRequest> = [];\n      const serverConfigLayer = ServerConfig.ServerConfig.layerTest(process.cwd(), {\n        prefix: \"t3-telemetry-base-\",\n      });\n",
      "after": "      };\n    }>;\n  } | null;\n}\n\nit.layer(NodeServices.layer)(\"AnalyticsService test\", (it) => {\n  it.effect(\"ignores attempts to enable analytics and flush sensitive event properties\", () =>\n    Effect.gen(function* () {\n      const capturedRequests: Array<RecordedBatchRequest> = [];\n      const serverConfigLayer = ServerConfig.ServerConfig.layerTest(process.cwd(), {\n        prefix: \"t3-telemetry-base-\",\n      });\n"
    },
    {
      "path": "apps/server/src/telemetry/AnalyticsService.test.ts",
      "before": "        const telemetryIdentifier = yield* getTelemetryIdentifier;\n        assert.equal(telemetryIdentifier !== null, true);\n        const analytics = yield* AnalyticsService.AnalyticsService;\n\n        for (let index = 0; index < 45; index += 1) {\n          yield* analytics.record(\"test.flush.drain\", { index });\n        }\n\n        yield* analytics.flush;\n      }).pipe(Effect.provide(runtimeLayer));\n\n      const batchRequests = capturedRequests.filter(\n        (request): request is RecordedBatchRequest & { readonly body: RecordedBatchBody } =>\n          Array.isArray(request.body?.batch),\n      );\n      assert.equal(batchRequests.length, 3);\n      assert.equal(\n        batchRequests.every(\n          (request) => request.path.endsWith(\"/batch/\") || request.path.endsWith(\"/batch\"),\n        ),\n        true,\n      );\n      const deliveredIndexes = batchRequests.flatMap((request) =>\n        request.body.batch\n          .filter((event) => event.event === \"test.flush.drain\")\n          .map((event) => event.properties?.index)\n          .filter((index): index is number => typeof index === \"number\"),\n      );\n\n      const sorted = deliveredIndexes.toSorted((a, b) => a - b);\n      assert.equal(sorted.length, 45);\n      assert.deepEqual(\n        sorted,\n        Array.from({ length: 45 }, (_, index) => index),\n      );\n      assert.equal(\n        batchRequests.every((request) =>\n          request.body.batch.every((event) => event.properties?.clientType === \"cli-web-client\"),\n        ),\n        true,\n      );\n      assert.equal(\n        batchRequests.every((request) =>\n          request.body.batch.every(\n            (event) =>\n              event.properties?.serverOs === \"Linux\" &&\n              event.properties.serverArch === \"arm64\" &&\n              event.properties.serverAppVersion === event.properties.t3CodeVersion &&\n              event.properties.serverMode === \"web\",\n          ),\n        ),\n        true,\n      );\n    }),\n  );\n\n  it.effect(\"does not send batch requests when telemetry is disabled\", () =>\n    Effect.gen(function* () {\n",
      "after": "        const telemetryIdentifier = yield* getTelemetryIdentifier;\n        assert.equal(telemetryIdentifier !== null, true);\n        const analytics = yield* AnalyticsService.AnalyticsService;\n\n        for (let index = 0; index < 45; index += 1) {\n          yield* analytics.record(\"test.flush.drain\", { index, sourceCode: \"company secret\" });\n        }\n\n        yield* analytics.flush;\n      }).pipe(Effect.provide(runtimeLayer));\n\n      assert.deepEqual(capturedRequests, []);\n    }),\n  );\n\n  it.effect(\"does not send batch requests when telemetry is disabled\", () =>\n    Effect.gen(function* () {\n"
    },
    {
      "path": "apps/server/src/telemetry/AnalyticsService.ts",
      "before": "/**\n * Anonymous PostHog telemetry service.\n *\n * Persists an installation-scoped anonymous identifier, buffers events in\n * memory, and flushes batches over Effect's HTTP client.\n",
      "after": "import { allowAnalyticsExport } from \"@t3tools/shared/t2Analytics\";\n/**\n * Anonymous PostHog telemetry service.\n *\n * Persists an installation-scoped anonymous identifier, buffers events in\n * memory, and flushes batches over Effect's HTTP client.\n"
    },
    {
      "path": "apps/server/src/telemetry/AnalyticsService.ts",
      "before": "  }\n}\n\n/** @public Service construction is part of the canonical Effect module API. */\nexport const make = Effect.gen(function* () {\n  const telemetryConfig = yield* TelemetryEnvConfig;\n  const httpClient = yield* HttpClient.HttpClient;\n  const serverConfig = yield* ServerConfig.ServerConfig;\n  const identifier = yield* getTelemetryIdentifier;\n  const bufferRef = yield* Ref.make<ReadonlyArray<BufferedAnalyticsEvent>>([]);\n",
      "after": "  }\n}\n\n/** @public Service construction is part of the canonical Effect module API. */\nexport const make = Effect.gen(function* () {\n  if (!allowAnalyticsExport()) {\n    return AnalyticsService.of({ record: () => Effect.void, flush: Effect.void });\n  }\n  const telemetryConfig = yield* TelemetryEnvConfig;\n  const httpClient = yield* HttpClient.HttpClient;\n  const serverConfig = yield* ServerConfig.ServerConfig;\n  const identifier = yield* getTelemetryIdentifier;\n  const bufferRef = yield* Ref.make<ReadonlyArray<BufferedAnalyticsEvent>>([]);\n"
    },
    {
      "path": "apps/web/src/components/clerk/BrowserManagedAuthShell.tsx",
      "before": "}: {\n  readonly publishableKey: string;\n  readonly children: ReactNode;\n}) {\n  return (\n    <ClerkProvider appearance={clerkAppearance} publishableKey={publishableKey}>\n      <ManagedRelayAuthProvider>{children}</ManagedRelayAuthProvider>\n    </ClerkProvider>\n  );\n}\n",
      "after": "}: {\n  readonly publishableKey: string;\n  readonly children: ReactNode;\n}) {\n  return (\n    <ClerkProvider telemetry={false} appearance={clerkAppearance} publishableKey={publishableKey}>\n      <ManagedRelayAuthProvider>{children}</ManagedRelayAuthProvider>\n    </ClerkProvider>\n  );\n}\n"
    },
    {
      "path": "apps/web/src/components/clerk/ElectronManagedAuthShell.tsx",
      "before": "}: {\n  readonly publishableKey: string;\n  readonly children: ReactNode;\n}) {\n  return (\n    <ClerkProvider appearance={clerkAppearance} publishableKey={publishableKey} passkeys={passkeys}>\n      <ManagedRelayAuthProvider>{children}</ManagedRelayAuthProvider>\n    </ClerkProvider>\n  );\n}\n",
      "after": "}: {\n  readonly publishableKey: string;\n  readonly children: ReactNode;\n}) {\n  return (\n    <ClerkProvider\n      telemetry={false}\n      appearance={clerkAppearance}\n      publishableKey={publishableKey}\n      passkeys={passkeys}\n    >\n      <ManagedRelayAuthProvider>{children}</ManagedRelayAuthProvider>\n    </ClerkProvider>\n  );\n}\n"
    },
    {
      "path": "packages/shared/package.json",
      "before": "{\n  \"name\": \"@t3tools/shared\",\n  \"private\": true,\n  \"type\": \"module\",\n  \"exports\": {\n    \"./legacyCliLauncher\": {\n      \"types\": \"./src/legacyCliLauncher.ts\",\n      \"import\": \"./src/legacyCliLauncher.ts\"\n    },\n    \"./delimitedPreview\": {\n",
      "after": "{\n  \"name\": \"@t3tools/shared\",\n  \"private\": true,\n  \"type\": \"module\",\n  \"exports\": {\n    \"./t2Analytics\": {\n      \"types\": \"./src/t2Analytics.ts\",\n      \"import\": \"./src/t2Analytics.ts\"\n    },\n    \"./legacyCliLauncher\": {\n      \"types\": \"./src/legacyCliLauncher.ts\",\n      \"import\": \"./src/legacyCliLauncher.ts\"\n    },\n    \"./delimitedPreview\": {\n"
    },
    {
      "path": "packages/shared/src/relayTracing.test.ts",
      "before": "\n      expect(userSpans).toEqual([\"relay.operation\"]);\n    }),\n  );\n\n  it.effect(\"preserves nested error causes in exported relay spans\", () => {\n    const fetchFn = vi.fn<typeof fetch>(async () => new Response(null, { status: 202 }));\n    const httpClientLayer = FetchHttpClient.layer.pipe(\n      Layer.provide(Layer.succeed(FetchHttpClient.Fetch, fetchFn)),\n    );\n    const tracingLayer = makeRelayClientTracingLayer(\n",
      "after": "\n      expect(userSpans).toEqual([\"relay.operation\"]);\n    }),\n  );\n\n  it.effect(\"keeps error causes local even with a configured relay exporter\", () => {\n    const fetchFn = vi.fn<typeof fetch>(async () => new Response(null, { status: 202 }));\n    const httpClientLayer = FetchHttpClient.layer.pipe(\n      Layer.provide(Layer.succeed(FetchHttpClient.Fetch, fetchFn)),\n    );\n    const tracingLayer = makeRelayClientTracingLayer(\n"
    },
    {
      "path": "packages/shared/src/relayTracing.test.ts",
      "before": "\n    return Layer.build(tracedApplication).pipe(\n      Effect.scoped,\n      Effect.andThen(\n        Effect.sync(() => {\n          expect(fetchFn).toHaveBeenCalledOnce();\n          const payload = new TextDecoder().decode(fetchFn.mock.calls[0]?.[1]?.body as Uint8Array);\n          expect(payload).toContain(\"relay request failed\");\n          expect(payload).toContain(\"relay socket closed\");\n        }),\n      ),\n    );\n  });\n});\n",
      "after": "\n    return Layer.build(tracedApplication).pipe(\n      Effect.scoped,\n      Effect.andThen(\n        Effect.sync(() => {\n          expect(fetchFn).not.toHaveBeenCalled();\n        }),\n      ),\n    );\n  });\n});\n"
    },
    {
      "path": "packages/shared/src/relayTracing.ts",
      "before": "import * as Cause from \"effect/Cause\";\nimport * as Context from \"effect/Context\";\nimport * as Effect from \"effect/Effect\";\nimport * as Exit from \"effect/Exit\";\nimport * as Layer from \"effect/Layer\";\n",
      "after": "import { allowAnalyticsExport } from \"./t2Analytics.ts\";\nimport * as Cause from \"effect/Cause\";\nimport * as Context from \"effect/Context\";\nimport * as Effect from \"effect/Effect\";\nimport * as Exit from \"effect/Exit\";\nimport * as Layer from \"effect/Layer\";\n"
    },
    {
      "path": "packages/shared/src/relayTracing.ts",
      "before": "\nexport function makeRelayClientTracingLayer(\n  config: RelayClientTracingConfig | null,\n  resource: RelayClientTracingResource,\n): Layer.Layer<never, never, HttpClient.HttpClient> {\n  if (config === null) {\n    return Layer.succeed(RelayClientTracer, Option.none());\n  }\n\n  const tracerLayer = OtlpTracer.layer({\n    url: config.tracesUrl,\n",
      "after": "\nexport function makeRelayClientTracingLayer(\n  config: RelayClientTracingConfig | null,\n  resource: RelayClientTracingResource,\n): Layer.Layer<never, never, HttpClient.HttpClient> {\n  if (!allowAnalyticsExport() || config === null) {\n    return Layer.succeed(RelayClientTracer, Option.none());\n  }\n\n  const tracerLayer = OtlpTracer.layer({\n    url: config.tracesUrl,\n"
    },
    {
      "path": "scripts/build-desktop-artifact.test.ts",
      "before": "    assert.equal(resolveDesktopUpdateChannel(\"0.0.17-nightly.20260413.42\"), \"nightly\");\n    assert.equal(resolveDesktopUpdateChannel(\"0.0.17\"), \"latest\");\n  });\n\n  it(\"switches desktop packaging product names to nightly for nightly builds\", () => {\n    assert.equal(resolveDesktopProductName(\"0.0.17\"), \"T3 Code (Alpha)\");\n    assert.equal(resolveDesktopProductName(\"0.0.17-nightly.20260413.42\"), \"T3 Code (Nightly)\");\n  });\n\n  it(\"switches desktop packaging icons to the nightly artwork for nightly versions\", () => {\n    assert.deepStrictEqual(resolveDesktopBuildIconAssets(\"0.0.17\"), {\n      macIconPng: BRAND_ASSET_PATHS.productionMacIconPng,\n",
      "after": "    assert.equal(resolveDesktopUpdateChannel(\"0.0.17-nightly.20260413.42\"), \"nightly\");\n    assert.equal(resolveDesktopUpdateChannel(\"0.0.17\"), \"latest\");\n  });\n\n  it(\"switches desktop packaging product names to nightly for nightly builds\", () => {\n    assert.equal(resolveDesktopProductName(\"0.0.17\"), \"T2 Code\");\n    assert.equal(resolveDesktopProductName(\"0.0.17-nightly.20260413.42\"), \"T2 Code (Nightly)\");\n  });\n\n  it(\"switches desktop packaging icons to the nightly artwork for nightly versions\", () => {\n    assert.deepStrictEqual(resolveDesktopBuildIconAssets(\"0.0.17\"), {\n      macIconPng: BRAND_ASSET_PATHS.productionMacIconPng,\n"
    },
    {
      "path": "scripts/build-desktop-artifact.test.ts",
      "before": "        \"**/node_modules/.bin\",\n        \"**/node_modules/.bin/**\",\n        \"**/*.map\",\n      ]);\n      assert.deepStrictEqual(mac.dmg, {\n        title: \"T3 Code (Alpha) 1.2.3 Installer\",\n        background: \"dmg/dmg-background-latest.png\",\n        window: { width: 640, height: 432 },\n        contents: [\n          { x: 166, y: 214, type: \"file\" },\n          { x: 474, y: 214, type: \"link\", path: \"/Applications\" },\n",
      "after": "        \"**/node_modules/.bin\",\n        \"**/node_modules/.bin/**\",\n        \"**/*.map\",\n      ]);\n      assert.deepStrictEqual(mac.dmg, {\n        title: \"T2 Code 1.2.3 Installer\",\n        background: \"dmg/dmg-background-latest.png\",\n        window: { width: 640, height: 432 },\n        contents: [\n          { x: 166, y: 214, type: \"file\" },\n          { x: 474, y: 214, type: \"link\", path: \"/Applications\" },\n"
    },
    {
      "path": "scripts/build-desktop-artifact.test.ts",
      "before": "      T3CODE_MACOS_PROVISIONING_PROFILE: \"/tmp/t3code.provisionprofile\",\n      T3CODE_CLERK_PUBLISHABLE_KEY: `pk_test_${btoa(\"example.clerk.accounts.dev$\")}`,\n    });\n\n    assert.deepStrictEqual(configuration, {\n      appId: \"com.t3tools.t3code\",\n      teamId: \"ABC1234567\",\n      rpDomains: [\"example.clerk.accounts.dev\"],\n      provisioningProfilePath: \"/tmp/t3code.provisionprofile\",\n    });\n  });\n",
      "after": "      T3CODE_MACOS_PROVISIONING_PROFILE: \"/tmp/t3code.provisionprofile\",\n      T3CODE_CLERK_PUBLISHABLE_KEY: `pk_test_${btoa(\"example.clerk.accounts.dev$\")}`,\n    });\n\n    assert.deepStrictEqual(configuration, {\n      appId: \"com.shirubasoft.t2code\",\n      teamId: \"ABC1234567\",\n      rpDomains: [\"example.clerk.accounts.dev\"],\n      provisioningProfilePath: \"/tmp/t3code.provisionprofile\",\n    });\n  });\n"
    },
    {
      "path": "scripts/build-desktop-artifact.test.ts",
      "before": "\n    assert.deepStrictEqual(configuration.rpDomains, [\n      \"clerk.example.com\",\n      \"example.clerk.accounts.dev\",\n    ]);\n    assert.include(entitlements, \"<string>ABC1234567.com.t3tools.t3code</string>\");\n    assert.include(entitlements, \"<string>webcredentials:clerk.example.com</string>\");\n    assert.include(entitlements, \"<string>webcredentials:example.clerk.accounts.dev</string>\");\n    assert.include(entitlements, \"<key>com.apple.security.cs.allow-jit</key>\");\n  });\n\n",
      "after": "\n    assert.deepStrictEqual(configuration.rpDomains, [\n      \"clerk.example.com\",\n      \"example.clerk.accounts.dev\",\n    ]);\n    assert.include(entitlements, \"<string>ABC1234567.com.shirubasoft.t2code</string>\");\n    assert.include(entitlements, \"<string>webcredentials:clerk.example.com</string>\");\n    assert.include(entitlements, \"<string>webcredentials:example.clerk.accounts.dev</string>\");\n    assert.include(entitlements, \"<key>com.apple.security.cs.allow-jit</key>\");\n  });\n\n"
    },
    {
      "path": "scripts/build-desktop-artifact.test.ts",
      "before": "        entitlementsPath: \"/tmp/entitlements.mac.plist\",\n        provisioningProfilePath: \"/tmp/t3code.provisionprofile\",\n      });\n\n      const mac = config.mac as Record<string, unknown>;\n      assert.equal(config.appId, \"com.t3tools.t3code\");\n      assert.equal(mac.entitlements, \"/tmp/entitlements.mac.plist\");\n      assert.equal(mac.provisioningProfile, \"/tmp/t3code.provisionprofile\");\n      assert.match(String(mac.sign), /[\\\\/]scripts[\\\\/]sign-macos\\.ts$/);\n      assert.deepStrictEqual(mac.protocols, [\n        { name: \"T3 Code\", schemes: [\"t3code\", \"t3code-dev\"] },\n",
      "after": "        entitlementsPath: \"/tmp/entitlements.mac.plist\",\n        provisioningProfilePath: \"/tmp/t3code.provisionprofile\",\n      });\n\n      const mac = config.mac as Record<string, unknown>;\n      assert.equal(config.appId, \"com.shirubasoft.t2code\");\n      assert.equal(mac.entitlements, \"/tmp/entitlements.mac.plist\");\n      assert.equal(mac.provisioningProfile, \"/tmp/t3code.provisionprofile\");\n      assert.match(String(mac.sign), /[\\\\/]scripts[\\\\/]sign-macos\\.ts$/);\n      assert.deepStrictEqual(mac.protocols, [\n        { name: \"T3 Code\", schemes: [\"t3code\", \"t3code-dev\"] },\n"
    },
    {
      "path": "scripts/build-desktop-artifact.ts",
      "before": "import * as Stream from \"effect/Stream\";\nimport { Command, Flag } from \"effect/unstable/cli\";\nimport { ChildProcess, ChildProcessSpawner } from \"effect/unstable/process\";\n\nconst LINUX_ICON_SIZES = [16, 22, 24, 32, 48, 64, 128, 256, 512] as const;\nconst DESKTOP_APP_ID = \"com.t3tools.t3code\";\nconst APPLE_TEAM_ID_PATTERN = /^[A-Z0-9]{10}$/u;\n\nconst BuildPlatform = Schema.Literals([\"mac\", \"linux\", \"win\"]);\nconst BuildArch = Schema.Literals([\"arm64\", \"x64\", \"universal\"]);\n\n",
      "after": "import * as Stream from \"effect/Stream\";\nimport { Command, Flag } from \"effect/unstable/cli\";\nimport { ChildProcess, ChildProcessSpawner } from \"effect/unstable/process\";\n\nconst LINUX_ICON_SIZES = [16, 22, 24, 32, 48, 64, 128, 256, 512] as const;\nconst DESKTOP_APP_ID = \"com.shirubasoft.t2code\";\nconst APPLE_TEAM_ID_PATTERN = /^[A-Z0-9]{10}$/u;\n\nconst BuildPlatform = Schema.Literals([\"mac\", \"linux\", \"win\"]);\nconst BuildArch = Schema.Literals([\"arm64\", \"x64\", \"universal\"]);\n\n"
    },
    {
      "path": "scripts/build-desktop-artifact.ts",
      "before": "\n  return `${trimmed.slice(0, versionSeparator)}/${trimmed.slice(versionSeparator + 1)}`;\n}\n\nexport function resolveDesktopProductName(version: string): string {\n  return resolveDesktopUpdateChannel(version) === \"nightly\"\n    ? \"T3 Code (Nightly)\"\n    : (desktopPackageJson.productName ?? \"T3 Code\");\n}\n\nexport const createBuildConfig = Effect.fn(\"createBuildConfig\")(function* (\n  platform: typeof BuildPlatform.Type,\n  target: string,\n",
      "after": "\n  return `${trimmed.slice(0, versionSeparator)}/${trimmed.slice(versionSeparator + 1)}`;\n}\n\nexport function resolveDesktopProductName(version: string): string {\n  return resolveDesktopUpdateChannel(version) === \"nightly\" ? \"T2 Code (Nightly)\" : \"T2 Code\";\n}\n\nexport const createBuildConfig = Effect.fn(\"createBuildConfig\")(function* (\n  platform: typeof BuildPlatform.Type,\n  target: string,\n"
    },
    {
      "path": "scripts/build-desktop-artifact.ts",
      "before": "  arch?: typeof BuildArch.Type,\n) {\n  const buildConfig: Record<string, unknown> = {\n    appId: DESKTOP_APP_ID,\n    productName: resolveDesktopProductName(version),\n    artifactName: \"T3-Code-${version}-${arch}.${ext}\",\n    electronLanguages: [...DESKTOP_ELECTRON_LANGUAGES],\n    files: [\n      ...DESKTOP_FILE_EXCLUSIONS,\n      ...(platform === \"mac\"\n        ? resolveMacFileExclusions(arch)\n",
      "after": "  arch?: typeof BuildArch.Type,\n) {\n  const buildConfig: Record<string, unknown> = {\n    appId: DESKTOP_APP_ID,\n    productName: resolveDesktopProductName(version),\n    artifactName: \"T2-Code-${version}-${arch}.${ext}\",\n    electronLanguages: [...DESKTOP_ELECTRON_LANGUAGES],\n    files: [\n      ...DESKTOP_FILE_EXCLUSIONS,\n      ...(platform === \"mac\"\n        ? resolveMacFileExclusions(arch)\n"
    },
    {
      "path": "packages/shared/src/cliRelease.ts",
      "before": "const CLI_RELEASE_REPOSITORY = \"pingdotgg/t3code\";",
      "after": "const CLI_RELEASE_REPOSITORY = \"shirubasoft/t2code\";"
    },
    {
      "path": "packages/shared/src/cliRelease.test.ts",
      "before": "\"https://github.com/pingdotgg/t3code/releases/download/v1.2.3\"",
      "after": "\"https://github.com/shirubasoft/t2code/releases/download/v1.2.3\""
    },
    {
      "path": "packages/shared/src/cliRelease.test.ts",
      "before": "\"https://api.github.com/repos/pingdotgg/t3code/releases?per_page=100&page=1\"",
      "after": "\"https://api.github.com/repos/shirubasoft/t2code/releases?per_page=100&page=1\""
    },
    {
      "path": "scripts/install.sh",
      "before": "repo=\"pingdotgg/t3code\"",
      "after": "repo=\"shirubasoft/t2code\""
    },
    {
      "path": "scripts/install.ps1",
      "before": "$repo = \"pingdotgg/t3code\"",
      "after": "$repo = \"shirubasoft/t2code\""
    },
    {
      "path": "apps/web/src/components/desktopUpdate.logic.ts",
      "before": "const DESKTOP_RELEASE_HISTORY_URL = \"https://github.com/pingdotgg/t3code/releases\";",
      "after": "const DESKTOP_RELEASE_HISTORY_URL = \"https://github.com/shirubasoft/t2code/releases\";"
    },
    {
      "path": "apps/web/src/components/desktopUpdate.logic.test.ts",
      "before": "\"https://github.com/pingdotgg/t3code/releases/tag/v0.0.30\"",
      "after": "\"https://github.com/shirubasoft/t2code/releases/tag/v0.0.30\""
    },
    {
      "path": "apps/web/src/components/desktopUpdate.logic.test.ts",
      "before": "\"https://github.com/pingdotgg/t3code/releases/tag/v0.0.30-nightly.20260728.931\"",
      "after": "\"https://github.com/shirubasoft/t2code/releases/tag/v0.0.30-nightly.20260728.931\""
    },
    {
      "path": "apps/web/src/components/desktopUpdate.logic.test.ts",
      "before": "\"https://github.com/pingdotgg/t3code/releases\"",
      "after": "\"https://github.com/shirubasoft/t2code/releases\""
    },
    {
      "path": "packages/ssh/src/tunnel.test.ts",
      "before": "    assert.include(script, \"T3_NODE_SCRIPT_PATH=''\");\n    assert.include(\n      script,\n      \"T3_RELEASE_BASE_URL='https://github.com/pingdotgg/t3code/releases/download'\",\n    );\n    assert.include(script, 'T3_RUNTIME_DIR=\"$HOME/.t3/runtime/versions/$T3_ARCHIVE_VERSION\"');\n    assert.include(script, 'T3_ARCHIVE=\"t3-$T3_ARCHIVE_VERSION-$T3_PLATFORM-$T3_ARCH.tar.gz\"');\n",
      "after": "    assert.include(script, \"T3_NODE_SCRIPT_PATH=''\");\n    assert.include(\n      script,\n      \"T3_RELEASE_BASE_URL='https://github.com/shirubasoft/t2code/releases/download'\",\n    );\n    assert.include(script, 'T3_RUNTIME_DIR=\"$HOME/.t3/runtime/versions/$T3_ARCHIVE_VERSION\"');\n    assert.include(script, 'T3_ARCHIVE=\"t3-$T3_ARCHIVE_VERSION-$T3_PLATFORM-$T3_ARCH.tar.gz\"');\n"
    },
    {
      "path": "apps/web/src/components/desktopUpdate.toast.test.tsx",
      "before": "    link?.props.onClick?.();\n    await vi.waitFor(() => {\n      expect(openExternal).toHaveBeenCalledWith(\n        \"https://github.com/pingdotgg/t3code/releases/tag/v0.0.30\",\n      );\n    });\n    expect(testState.addToast).toHaveBeenCalledTimes(1);\n",
      "after": "    link?.props.onClick?.();\n    await vi.waitFor(() => {\n      expect(openExternal).toHaveBeenCalledWith(\n        \"https://github.com/shirubasoft/t2code/releases/tag/v0.0.30\",\n      );\n    });\n    expect(testState.addToast).toHaveBeenCalledTimes(1);\n"
    },
    {
      "path": "apps/web/src/components/desktopUpdate.toast.test.tsx",
      "before": "\n    await vi.waitFor(() => {\n      expect(openExternal).toHaveBeenCalledWith(\n        \"https://github.com/pingdotgg/t3code/releases/tag/v0.0.30\",\n      );\n    });\n  });\n",
      "after": "\n    await vi.waitFor(() => {\n      expect(openExternal).toHaveBeenCalledWith(\n        \"https://github.com/shirubasoft/t2code/releases/tag/v0.0.30\",\n      );\n    });\n  });\n"
    },
    {
      "path": "apps/web/src/components/sidebar/SidebarUpdateReleaseNotes.test.tsx",
      "before": "    );\n\n    expect(anchors.map(({ props }) => props.href)).toEqual([\n      \"https://github.com/pingdotgg/t3code/releases/tag/v0.0.36-nightly.3\",\n      \"https://github.com/pingdotgg/t3code/releases/tag/v0.0.36-nightly.2\",\n      \"https://github.com/pingdotgg/t3code/releases/tag/v0.0.36-nightly.1\",\n    ]);\n    expect(anchors.map(({ props }) => textContent(props.children))).toEqual([\n      \"View release on GitHub\",\n",
      "after": "    );\n\n    expect(anchors.map(({ props }) => props.href)).toEqual([\n      \"https://github.com/shirubasoft/t2code/releases/tag/v0.0.36-nightly.3\",\n      \"https://github.com/shirubasoft/t2code/releases/tag/v0.0.36-nightly.2\",\n      \"https://github.com/shirubasoft/t2code/releases/tag/v0.0.36-nightly.1\",\n    ]);\n    expect(anchors.map(({ props }) => textContent(props.children))).toEqual([\n      \"View release on GitHub\",\n"
    },
    {
      "path": "apps/web/src/components/sidebar/SidebarUpdateReleaseNotes.test.tsx",
      "before": "      }),\n    );\n\n    expect(anchors.at(-1)?.props.href).toBe(\"https://github.com/pingdotgg/t3code/releases\");\n    expect(textContent(anchors.at(-1)?.props.children)).toBe(\"1 older release on GitHub\");\n  });\n\n",
      "after": "      }),\n    );\n\n    expect(anchors.at(-1)?.props.href).toBe(\"https://github.com/shirubasoft/t2code/releases\");\n    expect(textContent(anchors.at(-1)?.props.children)).toBe(\"1 older release on GitHub\");\n  });\n\n"
    },
    {
      "path": "apps/web/src/components/sidebar/SidebarUpdateReleaseNotes.test.tsx",
      "before": "    expect(preventDefault).toHaveBeenCalledOnce();\n    await vi.waitFor(() => {\n      expect(openExternal).toHaveBeenCalledWith(\n        \"https://github.com/pingdotgg/t3code/releases/tag/v0.0.36-nightly.3\",\n      );\n      expect(testState.addToast).toHaveBeenCalledWith({\n        type: \"error\",\n",
      "after": "    expect(preventDefault).toHaveBeenCalledOnce();\n    await vi.waitFor(() => {\n      expect(openExternal).toHaveBeenCalledWith(\n        \"https://github.com/shirubasoft/t2code/releases/tag/v0.0.36-nightly.3\",\n      );\n      expect(testState.addToast).toHaveBeenCalledWith({\n        type: \"error\",\n"
    },
    {
      "path": "scripts/install.sh",
      "before": "channel=\"${T3CODE_CHANNEL:-stable}\"",
      "after": "channel=\"${T3CODE_CHANNEL:-nightly}\""
    },
    {
      "path": "scripts/install.sh",
      "before": "(default: stable;",
      "after": "(default: nightly;"
    },
    {
      "path": "scripts/install.sh",
      "before": "T3CODE_CHANNEL=stable (the default)",
      "after": "T3CODE_CHANNEL=nightly (the default)"
    },
    {
      "path": "scripts/install.ps1",
      "before": "else { \"stable\" }",
      "after": "else { \"nightly\" }"
    },
    {
      "path": "scripts/install.ps1",
      "before": "(default: stable;",
      "after": "(default: nightly;"
    },
    {
      "path": "scripts/install.ps1",
      "before": "T3CODE_CHANNEL=stable (the default)",
      "after": "T3CODE_CHANNEL=nightly (the default)"
    },
    {
      "path": "apps/desktop/src/app/DesktopObservability.ts",
      "before": "    const loggerLayer = Logger.layer(\n      endpoints.logs === undefined\n",
      "after": "    const loggerLayer = Logger.layer(\n      !allowAnalyticsExport() || endpoints.logs === undefined\n"
    },
    {
      "path": "apps/server/src/serverLogger.ts",
      "before": "import { otlpSerializationLayer } from \"@t3tools/shared/observability\";",
      "after": "import { otlpSerializationLayer } from \"@t3tools/shared/observability\";\nimport { allowAnalyticsExport } from \"@t3tools/shared/t2Analytics\";"
    },
    {
      "path": "apps/server/src/serverLogger.ts",
      "before": "  const otlpLogger =\n    config.otlpLogsUrl === undefined\n",
      "after": "  const otlpLogger =\n    !allowAnalyticsExport() || config.otlpLogsUrl === undefined\n"
    },
    {
      "path": "apps/server/src/serverLogger.test.ts",
      "before": "\"exports log records to the configured logs endpoint\"",
      "after": "\"keeps log records local with a configured logs endpoint\""
    },
    {
      "path": "apps/server/src/serverLogger.test.ts",
      "before": "      assert.lengthOf(requests, 1);\n      const [request] = requests;\n      assert.strictEqual(request?.url, \"https://collector.example.com/v1/logs\");\n      assert.include(request?.body ?? \"\", \"server logger under test\");\n      assert.include(request?.body ?? \"\", \"t3-server\");\n      assert.include(request?.body ?? \"\", \"service.runtime\");",
      "after": "      assert.lengthOf(requests, 0);"
    },
    {
      "path": "apps/server/src/serverLogger.test.ts",
      "before": "\"sends the headers and wire format the rest of OTLP export already uses\"",
      "after": "\"does not export logs with protobuf and headers configured\""
    },
    {
      "path": "apps/server/src/serverLogger.test.ts",
      "before": "      assert.lengthOf(requests, 1);\n      assert.strictEqual(requests[0]?.headers[\"x-scope\"], \"logs\");\n      assert.strictEqual(requests[0]?.headers[\"content-type\"], \"application/x-protobuf\");",
      "after": "      assert.lengthOf(requests, 0);"
    },
    {
      "path": "apps/server/src/serverLogger.test.ts",
      "before": "\"stops duplicating messages onto the span once log records are exported\"",
      "after": "\"preserves local span events with a configured logs endpoint\""
    },
    {
      "path": "apps/server/src/serverLogger.test.ts",
      "before": "      assert.lengthOf(requests, 1);\n      assert.include(requests[0]?.body ?? \"\", \"server logger under test\");\n      assert.lengthOf(spans, 1);\n      assert.lengthOf(spans[0]?.events ?? [], 0);",
      "after": "      assert.lengthOf(requests, 0);\n      assert.lengthOf(spans, 1);\n      assert.deepEqual(\n        spans[0]?.events.map(([name]) => name),\n        [\"server logger under test\"],\n      );"
    },
    {
      "path": "apps/desktop/src/app/DesktopObservability.test.ts",
      "before": "\"exports main process log records to the configured logs endpoint\"",
      "after": "\"keeps main process logs local with a configured logs endpoint\""
    },
    {
      "path": "apps/desktop/src/app/DesktopObservability.test.ts",
      "before": "      assert.lengthOf(requests, 1);\n      const [request] = requests;\n      assert.strictEqual(request?.url, \"https://collector.example.com/v1/logs\");\n      assert.include(request?.body ?? \"\", \"desktop log export\");\n      assert.include(request?.body ?? \"\", \"service.runtime\");\n      assert.strictEqual(request?.headers[\"x-scope\"], \"desktop\");\n\n      // The log record is the export now, so the same message must not also\n      // ride along as an event on the span.",
      "after": "      assert.lengthOf(requests, 0);\n\n      // Log messages remain in the local trace even with an exporter configured."
    },
    {
      "path": "apps/desktop/src/app/DesktopObservability.test.ts",
      "before": "      assert.notEqual(record, undefined);\n      assert.lengthOf(record?.events ?? [], 0);",
      "after": "      assert.notEqual(record, undefined);\n      assert.deepEqual(\n        record?.events.map((event) => event.name),\n        [\"desktop log export\"],\n      );"
    },
    {
      "path": "apps/desktop/src/app/DesktopObservability.test.ts",
      "before": "\"reads every signal endpoint from Settings when the environment names none\"",
      "after": "\"does not export any signal when endpoints are configured in Settings\""
    },
    {
      "path": "apps/desktop/src/app/DesktopObservability.test.ts",
      "before": "      assert.deepEqual(requests.map((request) => request.url).toSorted(), [\n        \"https://settings.example.com/v1/logs\",\n        \"https://settings.example.com/v1/traces\",\n      ]);\n      assert.include(\n        requests.find((request) => request.url.endsWith(\"/v1/logs\"))?.body ?? \"\",\n        \"desktop log export from settings\",\n      );\n      assert.include(\n        requests.find((request) => request.url.endsWith(\"/v1/traces\"))?.body ?? \"\",\n        \"desktop-settings-export-test\",\n      );",
      "after": "      assert.deepEqual(requests, []);"
    },
    {
      "path": "scripts/release-smoke.ts",
      "before": "  NodeChildProcess.execFileSync(\n    process.execPath,\n    [\"--test\", NodePath.resolve(repoRoot, \".github/scripts/relay-state-output.test.cjs\")],\n    { stdio: \"inherit\" },\n  );",
      "after": "  const releaseWorkflow = NodeFS.readFileSync(\n    NodePath.resolve(repoRoot, \".github/workflows/release.yml\"),\n    \"utf8\",\n  );\n  for (const marker of [\n    \"Read production relay tracing config\",\n    \"T3CODE_RELAY_CLIENT_OTLP_TRACES_\",\n    \"relay-client-tracing-config\",\n    \"relay-client-tracing.env\",\n  ]) {\n    if (releaseWorkflow.includes(marker)) {\n      throw new Error(`Release workflow must not inject external tracing credentials: ${marker}`);\n    }\n  }"
    }
  ],
  "files": [
    "packages/shared/src/t2Analytics.ts",
    "apps/desktop/src/electron/t2UpdaterPrivacy.ts",
    "apps/server/src/observability/t2Privacy.test.ts",
    "apps/desktop/src/app/t2Privacy.test.ts",
    "apps/desktop/src/electron/t2UpdaterPrivacy.test.ts",
    ".gitmodules"
  ]
}
